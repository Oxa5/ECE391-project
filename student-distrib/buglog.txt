Buglog

Checkpoint5:
1.
Description:
	Another cursor show up when change terminal
Reason:
	Update cursor when write in schedule terminal
Times: 
	15minutes
2.
Description:
	The color did not show as I want
Reason:
	Change all ATTRIB to attrib except the backspace
Times: 
	15minutes

3.
Description:
	error: page fault
Reason:
	in page.c , 
	function remap_vidmap_page makes the memory[video page for terminal i] (VIDEO >> 12)+ 2 + terminal_num) correspond to the using 		memory in video_page_table and page_table
	but in init_paging, we forgot set them to .present = 1; at first. 
Times: 
	1hour

Checkpoint4:
1.
Description:
	Page fault when running fish
Reason:
	set_vid_page was not properly implemented. Didn't change the video_page_table
Times: 
	half day
2.
Description:
	open file fail when running cat and grep
Reason:
	d_read was not pproperly implemented. It adds a "\n" to every file name's tail when opening them.
Times: 
	2 hrs
3.
Description:
	page fault when running cat and grep
Reason:
	read_data's sanity check didn't cover all conditions
Times: 
	1 hrs
3.
Description:
	can not print the whole text when running"cat frame0/1.txt"
Reason:
	has the "i<128" limitation in for loop condition in function terminal_write in terminal.c 

	 for(i = 0; (i <nbytes)&&(i < 128)&&(((char*)buf)[i] != '\0') ; i++){
        putc(((char*)buf)[i]);
        update_cursor(get_screen_x(),get_screen_y());
     }
Times: 
	0.5 hrs
	
Checkpoint3:
1.
Description:
	Can't type anything after enter the shell. It shows I enter the terminal but it can not got the interrupt from keyboard.
Reason:
	I change the idt. Add idt[SYSCALL].reserved3 =1;idt[rtc].reserved3 =0;idt[keyboard].reserved3 =0;
Times: 
	2 hours. ಠ_ಠ
2.
Description:
	After type pingpong. The ball can not move.
Reason:
	There is something wrong with the rtc. In rtc handler, // if (rtc_counter ==(real_interrupt_freq / virtual_freq))  //if Reach the virtualization multiple
    // {
    //     rtc_interrupt = 1;//set interrupt signal as one times of interrupt
    //     rtc_counter = 0;//clean up 
    // } this is wrong
    if (rtc_counter % (real_interrupt_freq / virtual_freq) == 0) rtc_interrupt = 1;

Times: 
	30 minutes(I am not familiar with the rtc.) (^_^)
3.
Description:
	Page fault after enter the shell.
Reason:
	Set page wrong. We have struct PAGE_4MB and struct PAGE_4KB, when I write the set_page, I miss to use the struct PAGE_4MB.
Times: 
	1 hours. (´▽｀)
4.
Description:
	When read the filename, there is always something wrong. 
Reason:
	Didn't initite the filename to be {\0}. So each time there is something strange.
Times: 
	30 minutes. ಠ_ಠ Use gdb to print what is in the filename.
5.
Description:
	compile error. uint32_t process_[MAX_PROCESS_NUM]; can't work. 
Reason:
	Change to extern uint32_t process_[MAX_PROCESS_NUM]; and inite in .c file uint32_t process_[MAX_PROCESS_NUM] = {0};
Times: 
	10 minutes. Ask my classmate to help me. V(^_^)V
6.
Description:
	ls error. Each time print ls, it will return.
Reason:
	Let another teamate in our group to change the file_system, it was writen by him. And he change the function about directory.
Times: 
	4 hours.（･_･;）




Checkpoint2:
1.
Description:
	Can't compile. Shows that 'keyboard_buffer_num' undeclared (first use in this function).
	keyboard.c:156:error:'terminal_current' undeclared(first use in this function)
	undefined reference to 'terminal_current'
Reason:
	I think I clear this two things but still can not compile. So I choose to add a function to get the value.
	so add get_keyboard_buffer_num to fix the problem. 
Times: 
	30 minutes. ಠ_ಠ
2.
Description:
	tty2: Read only file
	mp3': Is a directory
Reason:
	Maybe devel has some problem and just open the devel again to solve the problem.
	Can't halt to close the devel.
Times: 
	15 minutes(Ask my classmate to fix the problem.) (^_^)
3.
Description:
	Cursor can not change with my character.
Reason:
	Change the putc function . In this function to update the cursor.
Times: 
	15 minutes. (´▽｀)
4.
Description:
	When I read the buffer from keyboard and write from the buf, there are some strange character.
Reason:
	Initial the buffer of buf before read. Because in while loop, I should refresh the buf to read from my keyboard buffer.
Times: 
	30 minutes. ಠ_ಠ
5.
Description:
	After add the refresh the keyboard buffer can't read anything from keyboard buffer to terminal buffer.
Reason:
	Refresh the keyboard buffer before call terminal read. Add a temp buffer to solve the problem.
Times: 
	10 minutes. Ask the TA. V(^_^)V
6.
Description:
	Very very strange problem. I can only write 128 bytes one time. Then each time i can only press random numbers characters.
	Can't find the reason. And I print the number of keyboard_buffer. It shows : 1 2 3 4 5 ... 15 16 99 100 ... 127
	And after press enter it shows : 1 2 120 121... 127. And I can't find the law. So i find a other way to solve the problem.
	At first I use terminal_current.keyboard_buffer_num and terminal_current.temp_buffer_num. I use the variable in struct terminal.
	But now I change them to static variable. Then they can behave correctly.
Reason:
	Maybe my variable can change randomly. Can't get the law of it.
Times: 
	4 hours. Very painful. （･_･;）
7.
Desciption:
	For all test results, the return values are all incorrect. All the results are exactly the opposite.
Reason:
	The case is PASS returns 0, FAIL returns -1. In the test.c line 18, we made a mistake, automatically assuming that PASS returns 1, FAIL returns 0.
Times:
	15 minutes.

8.
Desciption:
	When try to read an exe file, I can't see the beginning and the ending at the same time
Reason:
	Printed NULL
Times:
	3 hours

9.
Desciption:
	When calling read_directory, the file names in the later have the tail of the earlier names
Reason:
	Didn't clear the buffer after each call
Times:
	1 hours

10.
Desciption:
	When calling read_file_test_, meet page fault
Reason:
	Didn't set the condition when length is 0
Times:
	30 mins


Checkpoint1:

1. 
Description:
	Can't compile. It shows that I can't use the function in lib.c and looks very strange. Every time I see the code can see red line under the code.
Reason:
	Lose a head file. 
Times: 
	2 hours
2. 
Description:
	Can't enter into the test, press enter nothing happened
Reason:
	Write keyboard init wrong.
Times: 
	1 hour
	
3. 
Description:
	Can't add linkage file. Every time add the linkage file, it shows that there are multiple definition of variables.
Reason:
	Check the relationship of each file. And moving some definitions.
Times: 
	3 hours
	
4. 
Description:
	Can't use keyboard to show right word.
Reason:
	Didn't write the different case of shift and caps lock.
Times: 
	2 hours

5.
Description:
	File format error (.tx)->(.txt) when try to read file data in tests.c 
	->TEST_OUTPUT("file_system_test_5.1", read_data_test_((uint8_t*)"verylargetextwithverylongname.tx"));//why .tx?
Reason:
	TX files are typically binary file types used by some kind of software
Times:
	20min

6.
Desciption:
	test indicates that the error of the test result and the output result correspond to: The test succeeds with Result == FAIL and the test fails with Result == PASS
Reason:
	#define TEST_OUTPUT(name, result)	
	printf("[TEST %s] Result = %s\n", name, (result) ? "PASS" : "FAIL");//wrong
	as PASS == 0 == false ,and FAIL== -1 == true by definition, so we should exchange the position of "PASS" : "FAIL" to "FAIL":"PASS" to print right result.
Times:
	20min

	
